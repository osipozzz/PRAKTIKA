<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Управление товарами" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/app.ico">

    <Grid Margin="10" Validation.ErrorTemplate="{x:Null}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Форма товара -->
        <GroupBox Grid.Row="0" Header="Товар" Margin="0,0,0,10">
            <Grid Margin="10" Validation.ErrorTemplate="{x:Null}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=False}"
                         Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Цена:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding Price, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=False}"
                         Margin="0,0,0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=False}"
                         Margin="0,0,0,5"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Описание:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <TextBox Grid.Row="3" Grid.Column="1"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=False}"
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,5"/>

                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Добавить" Width="80" Margin="0,0,10,0"
                            Command="{Binding AddProductCommand}"/>
                    <Button Content="Изменить" Width="80" Margin="0,0,10,0"
                            Command="{Binding EditProductCommand}"/>
                    <Button Content="Удалить" Width="80" Margin="0,0,10,0"
                            Command="{Binding DeleteProductCommand}"/>
                    <Button Content="Очистить" Width="80"
                            Command="{Binding ClearFormCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Поиск -->
        <GroupBox Grid.Row="1" Header="Поиск" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Поиск:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1"
                         Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Очистить" Width="70" Margin="0,0,10,0"
                        Command="{Binding ClearSearchCommand}"/>
                <TextBlock Grid.Column="3" Text="{Binding SearchResultText}"
                           Margin="10,0,0,0" VerticalAlignment="Center" FontStyle="Italic"/>
            </Grid>
        </GroupBox>

        <!-- Список товаров -->
        <GroupBox Grid.Row="2" Header="Список товаров">
            <DataGrid x:Name="ProductsDataGrid"
                      ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:C2}}" Width="100"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Статус бар -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold"/>
            <TextBlock Text=" | Всего товаров: " Margin="10,0,0,0"/>
            <TextBlock Text="{Binding TotalCount}"/>
        </StackPanel>
    </Grid>
</Window>
